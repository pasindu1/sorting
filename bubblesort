import java.util.*;
class Bubblesort{
  
  public static void main(String args[]){
    Scanner input=new Scanner(System.in);
    int n = input.nextInt();
    int arr[]= {2,4,1,5,3,7};
    int temp;
    for(int j=0;j<n-2;){
    for(int i=0;i<n-2;){
      if(arr[i]>arr[i+1]){
        temp=arr[i];
        arr[i]=arr[i+1];
        arr[i+1]=temp;
      }else{
        i++;
      }
    }
    j++;
  }
    for(int k=0;k<n;k++){
      System.out.print(arr[k]+" , ");
    }
  }
  }
